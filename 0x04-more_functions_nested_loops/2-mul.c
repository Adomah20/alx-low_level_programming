#include <stdio.h>
#include "main.h"

/**
 * mul - multiplication of two integers
 * @a: first integer to be parse
 * @b: second integer to be parse
 * Return: multiplication of the two integers a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
