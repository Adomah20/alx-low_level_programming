#include "main.h"
/**
 * _abs - it compute the absolute value of an integer
 * @n: the integer given in the question
 * Return: the abs of the integer
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
